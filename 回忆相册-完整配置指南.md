# 回忆相册 · 完整配置指南

> 目标：做一个**长期可访问**的回忆相册。你布置好前端页面，朋友打开网址就能看，
> 也能随手传照片。全程**不需要服务器、不需要付费、朋友不用登录**。
>
> 本文件按「层次」组织，从原理 → 准备 → 每一步操作 → 完整代码 → 长期维护，逐层往下。
> 下次直接打开本文件照着做即可。

---

## 目录

- [第 0 层：整体原理（先看懂再动手）](#第-0-层整体原理先看懂再动手)
- [第 1 层：准备清单](#第-1-层准备清单)
- [第 2 层：GitHub 仓库与 Pages](#第-2-层github-仓库与-pages)
- [第 3 层：申请 GitHub Token](#第-3-层申请-github-token)
- [第 4 层：Cloudflare Worker 中间人](#第-4-层cloudflare-worker-中间人)
- [第 5 层：前端对接上传](#第-5-层前端对接上传)
- [第 6 层：自定义域名（可选但推荐）](#第-6-层自定义域名可选但推荐)
- [第 7 层：验证与日常使用](#第-7-层验证与日常使用)
- [第 8 层：长期维护与备份](#第-8-层长期维护与备份)
- [第 9 层：常见问题 FAQ](#第-9-层常见问题-faq)
- [附录 A：Worker 完整代码](#附录-aworker-完整代码)
- [附录 B：前端完整代码](#附录-b前端完整代码)
- [附录 C：环境变量速查表](#附录-c环境变量速查表)

---

## 第 0 层：整体原理（先看懂再动手）

### 架构图

```
        朋友 / 你（手机、电脑）
                 │  打开网址
                 ▼
   ┌─────────────────────────────┐
   │  你的静态网页（GitHub Pages） │   ← 你布置的前端，谁打开都能看
   │  相册展示 + 「上传」按钮      │
   └──────────────┬──────────────┘
                  │ 点上传，图片在后台发给中间人
                  ▼
   ┌─────────────────────────────┐
   │  Cloudflare Worker（中间人）  │   ← 免费、无到期
   │  内部藏着你的 GitHub Token    │   ← 密钥不暴露，安全
   └──────────────┬──────────────┘
                  │ 用 Token 调 GitHub API
                  ▼
   ┌─────────────────────────────┐
   │  你的 GitHub 仓库             │
   │  photos/ 文件夹存所有照片      │   ← 照片就是文件，永久留档
   └──────────────┬──────────────┘
                  │ 网页从 API 读取列表
                  ▼
              大家刷新就能看到新照片
```

### 数据流（一次上传发生了什么）

1. 朋友打开网页 → 直接看到你布置的相册（静态页面）。
2. 点「上传」选照片 → 网页在浏览器里**压缩图片**（省流量、省空间）。
3. 网页把图片（Base64）发给 **Worker 中间人**。
4. Worker 校验口令 → 用**藏在环境变量里的 Token** 调用 GitHub API，把图片提交进 `photos/`。
5. 网页重新读取照片列表 → **立刻显示**新照片（不用等 Pages 重建）。

### 为什么这样设计（为了"几十年还能打开"）

- **纯静态网页**：没有服务器，就没有"服务器不稳定 / 到期 / 涨价"的问题。
- **照片以文件存进仓库**：跟着代码走，Git 永久保留历史，可随时恢复。
- **Worker 藏 Token**：网页源码里看不到密钥，不怕被偷。
- **可换域名、可搬家**：域名是你的，将来想换平台随时搬。
- **多备份**：本地 clone + 别的平台镜像，任何一家倒了都不影响。

> 核心原则：**尽量不依赖任何会收费、会关停、会改政策的服务。**
> （所以不用微信小程序 + 云开发：它依赖腾讯，会涨价、要年审、政策会变。）

---

## 第 1 层：准备清单

动手前先确认你手上有这些：

| 序号 | 需要准备 | 说明 | 是否必须 |
|---|---|---|---|
| 1 | GitHub 账号 | 你已经有了 | 必须 |
| 2 | 一个相册仓库 | 放前端页面 + 照片 | 必须 |
| 3 | GitHub 细粒度 Token | 只授权相册仓库，Contents 读写 | 必须 |
| 4 | Cloudflare 账号 | 免费注册，邮箱即可 | 必须 |
| 5 | 一个上传口令 | 自己定一串字，只有小圈子知道 | 建议 |
| 6 | 域名（可选） | 约 ¥60/年，URL 永久属于你 | 推荐 |
| 7 | 前端页面代码 | 用 Gemini 做的相册前端 | 必须 |

**建议一次性准备好的信息**（配置时要用）：

- GitHub 用户名：`__________`
- 仓库名：`__________`（例如 `memory-album`）
- 分支：`main`
- 照片文件夹：`photos`
- 上传口令：`__________`
- Worker 网址：`https://__________.workers.dev`（第 4 层生成后回填）

---

## 第 2 层：GitHub 仓库与 Pages

### 2.1 新建仓库

1. 登录 GitHub → 右上角 `+` → **New repository**。
2. Repository name 填 `memory-album`（名字随意）。
3. 可见性选 **Public**（GitHub Pages 免费版需要公开仓库）。
4. 勾选 **Add a README file**。
5. 点 **Create repository**。

### 2.2 建立目录结构

在仓库里建好这个结构（现在只建文件夹，内容后加）：

```
memory-album/
├── index.html          ← 你的前端页面（Gemini 做的）
├── photos/             ← 所有照片都放这里
│   └── (照片会自动出现在这里)
└── README.md
```

建 `photos` 文件夹的办法：仓库页 → **Add file** → **Create new file** → 输入 `photos/.gitkeep` → 提交。
（`.gitkeep` 是个占位空文件，让 Git 保留空文件夹；之后有照片了可以删掉。）

### 2.3 上传前端页面

1. 仓库页 → **Add file** → **Upload files**。
2. 把 Gemini 生成的 `index.html`（以及相关 css/js 文件）拖进去。
3. 提交（Commit changes）。

### 2.4 开启 GitHub Pages

1. 仓库 → **Settings** → 左侧 **Pages**。
2. **Source** 选 `Deploy from a branch`。
3. **Branch** 选 `main`，目录选 `/ (root)`，点 **Save**。
4. 等 1–2 分钟，页面顶部会出现网址：
   ```
   https://你的用户名.github.io/memory-album/
   ```
5. 打开这个网址，能看到你的前端页面就成功了。

> 这个网址就是"临时网址"。等第 6 层买了域名再绑上去。

---

## 第 3 层：申请 GitHub Token

> 这个 Token 是"钥匙"，让 Worker 能替你往仓库里写照片。
> **绝不能写进网页源码，绝不能提交进仓库**，只放在 Worker 的环境变量里。

### 3.1 进入 Token 页面

1. 登录 GitHub → 右上角头像 → **Settings**。
2. 左侧拉到最底 → **Developer settings**。
3. 左侧 → **Personal access tokens** → **Fine-grained tokens**。
4. 点 **Generate new token**。

### 3.2 填写 Token 信息

| 字段 | 填什么 |
|---|---|
| Token name | `memory-album-worker` |
| Expiration | 选 1 年（到期重新生成，更安全） |
| Resource owner | 你的账号 |
| Repository access | 选 **Only select repositories** → 只勾 `memory-album` |

### 3.3 设置权限

展开 **Repository permissions**，只改这一项：

- **Contents** → 选 **Read and write**
- （Metadata 会自动变成 Read-only，正常）

其余全部保持 **No access**。

### 3.4 生成并保存

1. 点 **Generate token**。
2. 页面上会显示一串 `github_pat_...` 开头的字符。
3. **立刻复制**（离开页面就再也看不到了）。
4. 先临时粘贴到记事本，第 4 层要用。

> 安全提醒：这串东西相当于仓库钥匙，别发群里、别截图外传、别写进代码。
> 万一泄露：回到这个页面把旧 Token 删掉，重新生成一个，替换 Worker 里的即可。

---

## 第 4 层：Cloudflare Worker 中间人

> Worker 是"中间人"：帮你保管 Token，接收网页发来的图片，再提交进 GitHub。
> 朋友感知不到它的存在，只看到"上传成功了"。

### 4.1 注册 Cloudflare

1. 打开 https://dash.cloudflare.com/sign-up
2. 用邮箱注册（免费，不需要买任何东西）。
3. 验证邮箱后登录。

### 4.2 创建 Worker

1. 左侧菜单 → **Workers & Pages**。
2. 点 **Create application**（或 **Create**）。
3. 选 **Create Worker**（不要选 Pages）。
4. 给它起个名字，例如 `memory-album-uploader`。
5. 点 **Deploy**（先用默认的 Hello World 代码部署一次）。
6. 部署后点 **Edit code**（编辑代码）。

### 4.3 粘贴 Worker 代码

1. 把编辑器里的默认代码**全部删掉**。
2. 粘贴 [附录 A](#附录-aworker-完整代码) 里的完整代码。
3. 右上角点 **Deploy**。

### 4.4 配置环境变量（Secrets）★ 关键一步

回到 Worker 的 **Settings** → **Variables and Secrets** → 点 **Add**，
逐个添加下面的变量。类型选 **Secret**（敏感值，如 Token、口令）
或 **Text**（普通值，如仓库名）：

| 变量名 | 类型 | 值示例 | 说明 |
|---|---|---|---|
| `GH_TOKEN` | Secret | `github_pat_xxx...` | 第 3 层生成的 Token |
| `GH_OWNER` | Text | `你的GitHub用户名` | 仓库所有者 |
| `GH_REPO` | Text | `memory-album` | 仓库名 |
| `GH_BRANCH` | Text | `main` | 分支名 |
| `PHOTO_DIR` | Text | `photos` | 照片文件夹 |
| `UPLOAD_PASSWORD` | Secret | 你自己定的口令 | 防乱传，可留空表示不校验 |
| `ALLOW_ORIGIN` | Text | `*` | 允许的网页来源，先填 `*` 方便测试 |

每加完一个点 **Save / Deploy** 使其生效。

> `ALLOW_ORIGIN` 想更安全的话，填你的网址，例如
> `https://你的用户名.github.io`（多个用逗号分隔）。

### 4.5 拿到 Worker 网址

1. 回到 Worker 首页，顶部能看到网址，形如：
   ```
   https://memory-album-uploader.你的子域.workers.dev
   ```
2. 复制它，这就是**上传接口地址**，第 5 层要用。
3. 回填到第 1 层的准备清单。

### 4.6 测试 Worker 是否正常

浏览器直接打开这个地址（GET 请求会返回提示）：

```
https://你的worker地址.workers.dev/
```

看到一段 JSON（如 `{"ok":false,"error":"not found"}`）说明 Worker 活着。

再测试列表接口（能读到仓库 `photos/` 内容）：

```
https://你的worker地址.workers.dev/list
```

返回 `{"ok":true,"files":[...]}` 就说明 **Token 和仓库都配置正确**。
如果报错，看第 9 层 FAQ。

---

## 第 5 层：前端对接上传

> 这一步是把你 Gemini 做的前端和 Worker 接起来。
> 核心就三件事：① 压缩图片 ② 发给 Worker ③ 读取列表展示。

### 5.1 在页面里加配置

在你的 `index.html` 的 `<script>` 开头，改成你自己的值：

```js
const WORKER_URL = "https://你的worker地址.workers.dev"; // 第 4.5 步的地址
const UPLOAD_PASSWORD = "你定的口令";                      // 和 Worker 里一致
```

### 5.2 加一个上传按钮

```html
<input type="file" id="fileInput" accept="image/*" multiple hidden>
<button id="uploadBtn">上传照片</button>
```

### 5.3 接上完整逻辑

把 [附录 B](#附录-b前端完整代码) 里的 JS 粘贴进你的页面脚本，
并确保页面有个容器用来显示照片：

```html
<div id="gallery"></div>
```

页面加载时会自动调用 `/list` 拉取所有照片并显示；
点「上传照片」选图后会压缩、上传、然后**立刻刷新显示**。

> 更完整的代码见 [附录 B](#附录-b前端完整代码)。

---

## 第 6 层：自定义域名（可选但推荐）

> 有域名 = URL 永远属于你，将来换平台也能继续用。
> 域名每年续费约 ¥60，是唯一值得花的钱。

### 6.1 买域名

1. 注册商推荐：**阿里云（万网）**、**腾讯云**（支持支付宝/微信），
   或 **Cloudflare Registrar**（成本价、无加价，但域名必须用 Cloudflare DNS）。
2. 选后缀：优先 **`.com`**（最稳、几十年不易贬值）。
3. **重点看续费价**，别被"首年 1 元、续费 90"的套路坑。
4. 买完开启**自动续费**，并多充几年。
   - 域名过期被抢注，很难再拿回来。

### 6.2 绑定到 GitHub Pages

1. 仓库 → **Settings** → **Pages** → **Custom domain**。
2. 填入你的域名，例如 `memory-album.com`，点 **Save**。
   （GitHub 会自动在仓库里生成一个 `CNAME` 文件，不用手动建。）

### 6.3 到域名商配置 DNS

以 `memory-album.com` 为例：

**主域名（memory-album.com）**：添加 4 条 **A 记录**，都指向：

```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

**www 子域（www.memory-album.com）**：添加 1 条 **CNAME**，指向：

```
你的用户名.github.io
```

### 6.4 开启 HTTPS

1. 等 DNS 生效（几分钟到几小时）。
2. 回到 Pages 页面，勾选 **Enforce HTTPS**。

> 如果用 Cloudflare 管理 DNS：把对应记录的**小云朵关掉（灰云 / DNS only）**，
> 否则 GitHub 签不了 HTTPS 证书。

### 6.5 （可选）给 Worker 也绑域名

Worker 默认的 `workers.dev` 地址就够用，一般不用绑。
若想更整洁，可在 Worker → Settings → Domains & Routes 里绑一个子域，如 `upload.你的域名.com`。

---

## 第 7 层：验证与日常使用

### 7.1 上线前自测

1. 手机浏览器打开你的相册网址 → 能看到页面。
2. 点「上传照片」→ 选一张图 → 等提示成功。
3. 刷新页面 → 新照片出现。
4. 打开 GitHub 仓库 → `photos/` 里能看到刚上传的文件。

### 7.2 分享给朋友

- 直接把网址发给他们即可，**不需要账号、不需要口令**（看是公开的）。
- 上传时需要口令（如果 Worker 设了 `UPLOAD_PASSWORD`），
  把口令一起告诉他们，或在页面里预填好。

### 7.3 日常使用

- 看：随时打开网址。
- 传：打开网址点上传，图片自动压缩后进仓库。
- 管理：想删某张照片，直接去 GitHub 仓库 `photos/` 删掉那个文件即可。

---

## 第 8 层：长期维护与备份

> 目标：几十年后还能打开。做好下面几点，风险极低。

### 8.1 三份备份（重要）

1. **GitHub 主仓库**：主力。
2. **本地 clone**：电脑上执行一次，之后定期 `git pull`：
   ```
   git clone https://github.com/你的用户名/memory-album.git
   ```
3. **第二平台镜像**：把仓库同步到 GitLab 或 Gitee（可用导入功能），
   万一 GitHub 出问题还有一份。

### 8.2 域名续费

- 开启自动续费 + 余额充足 + 多买几年。
- 域名是所有环节里**最需要人盯**的一环。

### 8.3 Token 到期

- 细粒度 Token 设了 1 年有效期，到期上传会失败。
- 到期前重新生成一个新 Token，替换 Worker 里的 `GH_TOKEN` 即可（其它不用动）。

### 8.4 平台风险

- GitHub Pages、Cloudflare 都是大厂长期服务，但**没有绝对永久**。
- 因为网页是纯静态 + 照片是文件，**随时可以整体搬到任何新平台**，这是这套方案最大的价值。

---

## 第 9 层：常见问题 FAQ

**Q1：朋友需要 Token 或登录吗？**
不需要。看不用任何东西；传只需要口令（如果设了）。

**Q2：为什么不能把 Token 直接写在网页里？**
因为网页源码公开可见，而且 GitHub 会自动扫描公开仓库里的 Token 并**吊销**它。
所以必须藏在 Worker 里。

**Q3：`/list` 返回 `Bad credentials` 或 401？**
Token 不对或过期。重新生成 Token，更新 Worker 的 `GH_TOKEN`。

**Q4：`/list` 返回 `Not Found`？**
`GH_OWNER` / `GH_REPO` / `PHOTO_DIR` / `GH_BRANCH` 写错了，或 Token 没勾选这个仓库。

**Q5：上传报 `密码错误`？**
前端 `UPLOAD_PASSWORD` 和 Worker 里的不一致。

**Q6：上传报 413 / 图片太大？**
图片没压缩或太大。前端已内置压缩；也可以把压缩后的最大边长调小。

**Q7：网页报 CORS 错误？**
Worker 的 `ALLOW_ORIGIN` 要包含你的网页域名（测试阶段填 `*`）。

**Q8：新照片不显示？**
本项目是从 GitHub API 直接读列表，正常会立刻显示。
若显示旧内容，清一下浏览器缓存再试。

**Q9：能删照片吗？**
能。去 GitHub 仓库 `photos/` 删文件；或在 Worker 里加一个受口令保护的删除接口（按需再加）。

**Q10：会不会有人乱传/删我东西？**
设了 `UPLOAD_PASSWORD` 就挡住了陌生人乱传。
但**拿到口令的人有写权限**（GitHub Token 的 Contents 读写包含删除），
所以：口令只发小圈子；仓库只放照片；本地留备份，被破坏也能恢复。

---

## 附录 A：Worker 完整代码

> 用法：Cloudflare Worker 编辑代码处，全选删除后粘贴此代码，Deploy。
> 所有敏感值都通过第 4.4 层的环境变量提供，代码里不出现任何密钥。

```js
export default {
  async fetch(request, env) {
    const cors = {
      "Access-Control-Allow-Origin": env.ALLOW_ORIGIN || "*",
      "Access-Control-Allow-Methods": "GET,POST,OPTIONS",
      "Access-Control-Allow-Headers": "Content-Type",
    };

    if (request.method === "OPTIONS") {
      return new Response(null, { headers: cors });
    }

    const url = new URL(request.url);

    if (request.method === "GET" && url.pathname === "/list") {
      return handleList(env, cors);
    }
    if (request.method === "POST" && url.pathname === "/upload") {
      return handleUpload(request, env, cors);
    }

    return json({ ok: false, error: "not found" }, 404, cors);
  },
};

function json(obj, status, cors) {
  return new Response(JSON.stringify(obj), {
    status,
    headers: { ...cors, "Content-Type": "application/json" },
  });
}

function ghHeaders(env) {
  return {
    "Authorization": `Bearer ${env.GH_TOKEN}`,
    "Accept": "application/vnd.github+json",
    "User-Agent": "memory-album-worker",
  };
}

async function handleUpload(request, env, cors) {
  try {
    const body = await request.json();
    const { password, filename, content } = body;

    if (env.UPLOAD_PASSWORD && password !== env.UPLOAD_PASSWORD) {
      return json({ ok: false, error: "密码错误" }, 401, cors);
    }
    if (!filename || !content) {
      return json({ ok: false, error: "缺少 filename 或 content" }, 400, cors);
    }

    const safe = String(filename).replace(/[^\w.\-]/g, "_");
    const dir = env.PHOTO_DIR || "photos";
    const path = `${dir}/${Date.now()}-${safe}`;

    const api =
      `https://api.github.com/repos/${env.GH_OWNER}/${env.GH_REPO}/contents/${path}`;

    const res = await fetch(api, {
      method: "PUT",
      headers: { ...ghHeaders(env), "Content-Type": "application/json" },
      body: JSON.stringify({
        message: `upload ${path}`,
        content: content,               // 纯 Base64，不含 data: 前缀
        branch: env.GH_BRANCH || "main",
      }),
    });

    const data = await res.json();
    if (!res.ok) {
      return json({ ok: false, error: data.message || "GitHub 提交失败" }, 500, cors);
    }
    return json({
      ok: true,
      path,
      url: data.content && data.content.download_url,
    }, 200, cors);
  } catch (e) {
    return json({ ok: false, error: String(e) }, 500, cors);
  }
}

async function handleList(env, cors) {
  try {
    const dir = env.PHOTO_DIR || "photos";
    const ref = env.GH_BRANCH || "main";
    const api =
      `https://api.github.com/repos/${env.GH_OWNER}/${env.GH_REPO}/contents/${dir}?ref=${ref}`;

    const res = await fetch(api, { headers: ghHeaders(env) });
    const data = await res.json();

    if (!res.ok) {
      return json({ ok: false, error: data.message || "读取失败" }, 500, cors);
    }

    const files = (Array.isArray(data) ? data : [])
      .filter((f) =>
        f.type === "file" &&
        /\.(jpe?g|png|gif|webp|avif|heic|mp4|mov|webm)$/i.test(f.name)
      )
      .map((f) => ({ name: f.name, url: f.download_url }));

    return json({ ok: true, files }, 200, cors);
  } catch (e) {
    return json({ ok: false, error: String(e) }, 500, cors);
  }
}
```

---

## 附录 B：前端完整代码

> 这是最小可用的对接代码。可以整段替换 Gemini 页面里的脚本，
> 也可以只挑需要的部分拼进你的页面。记得先改开头的两个配置值。

```html
<!-- 页面里需要：一个上传按钮 + 一个照片容器 -->
<input type="file" id="fileInput" accept="image/*" multiple hidden>
<button id="uploadBtn">上传照片</button>
<div id="gallery"></div>

<script>
  // ====== 配置：改成你自己的 ======
  const WORKER_URL = "https://你的worker地址.workers.dev";
  const UPLOAD_PASSWORD = "你定的口令";
  // ===============================

  const fileInput = document.getElementById("fileInput");
  const uploadBtn = document.getElementById("uploadBtn");
  const gallery = document.getElementById("gallery");

  // 压缩图片：最长边不超过 maxSize，输出 JPEG
  async function compressImage(file, maxSize = 1600, quality = 0.82) {
    const bitmap = await createImageBitmap(file);
    let { width, height } = bitmap;
    const scale = Math.min(1, maxSize / Math.max(width, height));
    width = Math.round(width * scale);
    height = Math.round(height * scale);

    const canvas = document.createElement("canvas");
    canvas.width = width;
    canvas.height = height;
    canvas.getContext("2d").drawImage(bitmap, 0, 0, width, height);

    const blob = await new Promise((r) =>
      canvas.toBlob(r, "image/jpeg", quality)
    );
    const name = file.name.replace(/\.[^.]+$/, "") + ".jpg";
    return new File([blob], name, { type: "image/jpeg" });
  }

  // File -> 纯 Base64（去掉 data: 前缀）
  function fileToBase64(file) {
    return new Promise((resolve, reject) => {
      const r = new FileReader();
      r.onload = () => resolve(String(r.result).split(",")[1]);
      r.onerror = reject;
      r.readAsDataURL(file);
    });
  }

  // 上传单张
  async function uploadOne(file) {
    const compressed = await compressImage(file);
    const content = await fileToBase64(compressed);
    const res = await fetch(WORKER_URL + "/upload", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        password: UPLOAD_PASSWORD,
        filename: compressed.name,
        content,
      }),
    });
    return res.json();
  }

  // 读取列表并渲染
  async function loadGallery() {
    const res = await fetch(WORKER_URL + "/list");
    const data = await res.json();
    if (!data.ok) {
      console.error("加载失败:", data.error);
      return;
    }
    gallery.innerHTML = "";
    data.files
      .slice()
      .reverse()
      .forEach((f) => {
        const img = document.createElement("img");
        img.src = f.url;
        img.loading = "lazy";
        img.style.cssText =
          "width:200px;height:200px;object-fit:cover;margin:4px;border-radius:8px;";
        img.onclick = () => window.open(f.url, "_blank");
        gallery.appendChild(img);
      });
  }

  // 事件绑定
  uploadBtn.onclick = () => fileInput.click();

  fileInput.onchange = async () => {
    const files = Array.from(fileInput.files || []);
    if (!files.length) return;
    uploadBtn.disabled = true;
    uploadBtn.textContent = `上传中 0/${files.length}`;

    let done = 0;
    for (const file of files) {
      try {
        const r = await uploadOne(file);
        if (!r.ok) alert("上传失败：" + r.error);
      } catch (e) {
        alert("上传出错：" + e);
      }
      done++;
      uploadBtn.textContent = `上传中 ${done}/${files.length}`;
    }

    fileInput.value = "";
    uploadBtn.disabled = false;
    uploadBtn.textContent = "上传照片";
    await loadGallery();
  };

  // 首次进入加载
  loadGallery();
</script>
```

---

## 附录 C：环境变量速查表

在 Cloudflare Worker → Settings → Variables and Secrets 里添加：

| 变量名 | 类型 | 示例值 | 作用 |
|---|---|---|---|
| `GH_TOKEN` | Secret | `github_pat_xxx` | 访问 GitHub 的钥匙（第 3 层生成） |
| `GH_OWNER` | Text | `zhangsan` | 你的 GitHub 用户名 |
| `GH_REPO` | Text | `memory-album` | 相册仓库名 |
| `GH_BRANCH` | Text | `main` | 仓库分支 |
| `PHOTO_DIR` | Text | `photos` | 照片存放文件夹 |
| `UPLOAD_PASSWORD` | Secret | `mysecret123` | 上传口令（留空则不校验） |
| `ALLOW_ORIGIN` | Text | `*` 或 `https://你的用户名.github.io` | 允许访问的来源 |

---

## 一页速览（给未来的自己）

1. **建仓库** → 放 `index.html` + `photos/` → 开 Pages。
2. **生成 Token**（细粒度，只授权该仓库，Contents 读写）。
3. **建 Worker** → 贴附录 A 代码 → 配附录 C 变量 → Deploy。
4. **前端** 填 Worker 地址 + 口令 → 用附录 B 代码对接。
5. **买域名** → 绑 GitHub Pages（A 记录 + CNAME）→ 开 HTTPS。
6. **备份**：本地 clone + 第二平台镜像；**域名**开自动续费。
7. **Token 到期**（1 年）→ 重新生成，只换 Worker 里的 `GH_TOKEN`。

> 记住：网页是静态的、照片是文件、密钥在 Worker 里 —— 这套组合最耐时间。
